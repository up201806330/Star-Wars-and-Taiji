class MySpriteText{
    constructor(scene, text){
        this.scene = scene;
        this.text = text;

        // Cenas da spritesheet default da font, hardcoded aqui??
        // this.spritesheet = new MySpriteSheet("font.jpg", M, N);
    }

    getCharacterPosition(character){
        // Need to edit font spritesheet??
    }

    display(){
        //for (let c of this.text) this.spritesheet.activateCellP(this.getCharacterPosition(c));
        
    }
}